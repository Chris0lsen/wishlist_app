import { Grid } from '@chakra-ui/react';

import LoginButton from '../home/components/login-button';

const Home = () => {
  return (
    <Grid gap={4}>
      <LoginButton />
    </Grid>
  );
};

export default Home;
